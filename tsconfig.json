{
  "compilerOptions": {
    // --- Basic Options --- // --- Основные Опции ---
    "target": "ES2017", // Specify ECMAScript target version // Указываем целевую версию ECMAScript
    "lib": ["dom", "dom.iterable", "esnext"], // List of library files to be included in the compilation // Список файлов библиотек, включаемых в компиляцию
    "allowJs": true, // Allow javascript files to be compiled // Разрешаем компиляцию javascript файлов
    "skipLibCheck": true, // Skip type checking of declaration files // Пропускаем проверку типов декларационных файлов
    "strict": true, // Enable all strict type-checking options // Включаем все строгие опции проверки типов
    "noEmit": true, // Do not emit output (needed for Next.js) // Не генерировать выходные файлы (необходимо для Next.js)
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules // Включает совместимость генерации между CommonJS и ES модулями
    "module": "esnext", // Specify module code generation: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 'es2020', or 'ESNext' // Указываем генерацию кода модуля
    "moduleResolution": "bundler", // Specify module resolution strategy: 'node' or 'classic' ('bundler' is recommended for Next.js) // Указываем стратегию разрешения модулей ('bundler' рекомендуется для Next.js)
    "resolveJsonModule": true, // Enable importing .json files // Включаем импорт .json файлов
    "isolatedModules": true, // Transpile each file as a separate module // Транспилировать каждый файл как отдельный модуль
    "jsx": "preserve", // Preserve jsx output for frameworks like Next.js // Сохранять jsx для фреймворков типа Next.js
    "incremental": true, // Enable incremental compilation // Включаем инкрементальную компиляцию

    // --- Plugin Options --- // --- Опции Плагинов ---
    "plugins": [
      {
        "name": "next" // Enable Next.js plugin // Включаем плагин Next.js
      }
    ],

    // --- Path Aliases --- // --- Псевдонимы Путей ---
    "baseUrl": ".", // Base directory to resolve non-absolute module names // Базовая директория для разрешения неабсолютных имен модулей
    "paths": {
      "@/*": ["./src/*"] // Map '@/' to './src/' // Сопоставляем '@/' с './src/'
    }
  },
  // --- Files to Include --- // --- Включаемые Файлы ---
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  // --- Files to Exclude --- // --- Исключаемые Файлы ---
  "exclude": ["node_modules"] // Exclude node_modules directory // Исключаем директорию node_modules
}
